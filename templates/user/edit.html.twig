{% extends 'base.html.twig' %}

{% block title %}Modification utilisateur
{% endblock %}

{% block body %}
	<h1 class="text-center my-5 lightbeige">Modification du compte</h1>

	<div class="card w-75 mx-auto  border-card my-3 py-3 bg-lightbeige rounded">
		<div class="d-flex justify-content-end">
			<a class="btn btn-app me-5 my-2 px-5" href="{{ path('app_profile_show', {'id':user.id}) }}">Retour au compte</a>
		</div>
		{{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
	</div>
{% endblock %}
