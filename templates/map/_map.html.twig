{% block body %}
     <div >
     {% if map %}
             {{ ux_map(map, {style: 'height: 600px; width:50%; margin:2rem auto; border: 1px var(--dark-grey) solid'}) }}
    {% else %}
        <p>Pas de map à afficher</p>
     {% endif %}
     

    </div>
{% endblock %}
