<form method="post" action="{{ path('app_blog_comment_delete', {'id': blog_comment.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog_comment.id) }}">
    <button class="btn"><img src="{{ asset('../assets/images/icones/trash.svg') }}" alt="suppression"></button>
</form>
