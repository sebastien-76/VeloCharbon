<form method="post" action="{{ path('app_forum_comment_delete', {'id': forum_comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forum_comment.id) }}">
    <button class="btn">Delete</button>
</form>
