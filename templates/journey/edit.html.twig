{% extends 'base.html.twig' %}

{% block title %}Edition trajet
{% endblock %}

{% block body %}
	<h1 class="text-center my-5 lightbeige">Editer une étape</h1>
	<div class="py-3 bg-lightbeige container rounded">
		<div class="d-flex justify-content-end">
			<a class="btn btn-app me-5 my-2 px-5" href="{{ path('app_journey_show', {'id':journey.id}) }}">Retour au trajet</a>
		</div>
		{{ include('journey/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
	</div>
{% endblock %}
