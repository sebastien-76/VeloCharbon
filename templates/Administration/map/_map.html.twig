{% block body %}
     <div >
     {% if map %}
             {{ ux_map(map, {style: 'height: 600px; width:50%; margin:2rem auto;'}) }}
    {% else %}
        <p>Pas de map à afficher</p>
     {% endif %}
     

    </div>
{% endblock %}
